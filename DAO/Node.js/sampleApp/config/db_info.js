module.exports = (()=>{
    return {
        real: { // real server db info
            host: '*',
            port: '*',
            user: '*',
            password: '*',
            database: '*'
        }
    }
})();